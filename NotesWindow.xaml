<Window x:Class="Prog122_S24_L14_ImageEnum.NotesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prog122_S24_L14_ImageEnum"
        mc:Ignorable="d"
        Title="NotesWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <!-- Image Display -->
            <Image
                Name="DisplayImage"
                Height="200"
                Width="200"
                Stretch="Uniform"
                Margin="10" />

            <!-- Button to pick image file -->
            <Button
                Content="Pick Image"
                Click="PickImage_Click"
                Margin="10" />

            <!-- Enum ComboBox -->
            <ComboBox
                Name="ImageSizeComboBox"
                SelectionChanged="ImageSizeComboBox_SelectionChanged"
                Margin="10">
                <ComboBoxItem
                    Content="Small" />
                <ComboBoxItem
                    Content="Medium" />
                <ComboBoxItem
                    Content="Large" />
            </ComboBox>
        </StackPanel>
    </Grid>
</Window>
